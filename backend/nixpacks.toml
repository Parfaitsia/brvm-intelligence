[phases.install]
cmds = [
  "pip3 install -r backend/requirements.txt",
  "python3 -m playwright install chromium",
  "python3 -m playwright install-deps"
]

[start]
cmd = "cd backend && python3 -m uvicorn app.api.routes:app --host 0.0.0.0 --port $PORT"